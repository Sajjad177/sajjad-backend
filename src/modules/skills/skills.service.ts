import { ISkills } from "./skills.interface";

const createNewSkills = async (skillsData: ISkills) => {};

const getSkills = async () => {};

const updateSkills = async (skillsData: ISkills) => {};

const skillsService = {
  createNewSkills,
  getSkills,
  updateSkills,
};

export default skillsService;
